import Link from "next/link";
const periods = [["today", "اليوم"], ["yesterday", "أمس"], ["7d", "آخر 7 أيام"], ["30d", "آخر 30 يومًا"], ["month", "هذا الشهر"], ["lastMonth", "الشهر الماضي"]];
export function RangeFilter({ base, period, from, to }: { base: string; period: string; from?: string; to?: string }) {
  return <div className="space-y-4"><nav className="flex flex-wrap gap-2" aria-label="تصفية التاريخ">{periods.map(([key, label]) => <Link key={key} href={`${base}?period=${key}`} className={`rounded-lg border px-3 py-2 text-sm transition ${period === key ? "border-[#315b4c] bg-[#eaf2ed] font-semibold text-[#263b35]" : "border-slate-200 bg-white text-slate-600 hover:border-[#315b4c]"}`}>{label}</Link>)}</nav><form method="GET" action={base} className="flex flex-wrap items-end gap-2 text-sm"><input type="hidden" name="period" value="custom" /><label className="text-slate-600">من <input required type="date" name="from" defaultValue={from} className="mt-1 block max-w-full rounded-lg border border-slate-200 p-2.5 text-slate-900" /></label><label className="text-slate-600">إلى <input required type="date" name="to" defaultValue={to} className="mt-1 block max-w-full rounded-lg border border-slate-200 p-2.5 text-slate-900" /></label><button className="rounded-lg bg-[#263b35] px-4 py-2.5 font-medium text-white hover:bg-[#345348]">تطبيق</button></form></div>;
}

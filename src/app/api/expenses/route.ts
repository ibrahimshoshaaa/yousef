import { NextRequest, NextResponse } from "next/server";
import { z } from "zod";
import { requireAuth } from "@/lib/auth-helpers";
import { can } from "@/lib/rbac";
import { createExpense, listExpenses } from "@/services/finance.service";
const schema = z.object({ categoryId: z.string().min(1), amount: z.number().finite().positive(), date: z.coerce.date(), description: z.string().max(500).optional(), reference: z.string().max(200).optional() });
export async function GET() { try { const s = await requireAuth(); if (!can(s.role, "expenses.read")) return NextResponse.json({ error: "Forbidden" }, { status: 403 }); return NextResponse.json({ data: await listExpenses(s.storeId) }); } catch { return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); } }
export async function POST(req: NextRequest) { try { const s = await requireAuth(); if (!can(s.role, "expenses.write")) return NextResponse.json({ error: "Forbidden" }, { status: 403 }); const data = schema.parse(await req.json()); return NextResponse.json({ data: await createExpense({ ...data, storeId: s.storeId, userId: s.userId }) }, { status: 201 }); } catch (e) { if (e instanceof z.ZodError) return NextResponse.json({ error: "Invalid input", issues: e.issues }, { status: 422 }); if (e instanceof Error && e.message === "UNAUTHORIZED") return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); if (e instanceof Error && e.message === "Category not found") return NextResponse.json({ error: e.message }, { status: 404 }); console.error(e); return NextResponse.json({ error: "Internal server error" }, { status: 500 }); } }
